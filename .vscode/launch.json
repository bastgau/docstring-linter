{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Current file (debug mode)",
      "cwd": "${env:WORKSPACE_DIRECTORY}",
      "program": "${file}",
      "request": "launch",
      "type": "debugpy"
    }
  ]
}
